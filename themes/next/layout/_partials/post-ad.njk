<!-- 广告组件 - 适配Next主题+明暗模式 -->
<div class="next-ad-container" id="next-ad-container">
  <!-- 广告标签 -->
  <div class="ad-label">广告</div>
  <!-- 广告内容区域 -->
  <div class="ad-content" id="ad-body">
    <div class="ad-loading">
      广告正在加载中...
    </div>
  </div>
</div>

<!-- 配套CSS样式（建议放到Next主题的自定义样式文件中） -->
<style>
  /* 广告容器基础样式 */
  .next-ad-container {
    position: relative;
    margin: 20px 0;
    padding: 15px;
    border-radius: 8px;
    transition: background-color 0.3s ease, color 0.3s ease;
    /* 适配Next主题默认间距和圆角 */
    max-width: 100%;
    box-sizing: border-box;
  }

  /* 明模式（默认）样式 */
  .next-ad-container {
    background-color: #f5f5f5;
    color: #333;
    border: 1px solid #e6e6e6;
  }

  /* 暗模式样式（适配Next主题的dark模式） */
  [data-theme="dark"] .next-ad-container {
    background-color: #2c2c2c;
    color: #e5e5e5;
    border: 1px solid #444;
  }

  /* 广告标签样式 */
  .ad-label {
    position: absolute;
    top: 5px;
    right: 10px;
    font-size: 10px;
    opacity: 0.7;
    transition: color 0.3s ease;
  }

  [data-theme="dark"] .ad-label {
    color: #aaa;
  }

  /* 广告内容区域 */
  .ad-content {
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
  }

  /* 加载提示样式 */
  .ad-loading {
    font-size: 14px;
    opacity: 0.8;
  }

  /* 响应式适配（移动端优化） */
  @media (max-width: 768px) {
    .next-ad-container {
      padding: 10px;
    }
    .ad-content {
      height: 40px;
    }
    .ad-loading {
      font-size: 12px;
    }
  }
</style>

<!-- 可选：广告加载逻辑（如需动态加载广告内容可添加） -->
<script>
// 示例：模拟广告加载（可替换为实际广告接口请求）
document.addEventListener('DOMContentLoaded', function() {
  setTimeout(() => {
    const adBody = document.getElementById('ad-body');
    if (adBody) {
      // 替换为实际广告内容（如广告图片/链接）
      // adBody.innerHTML = '<a href="https://www.rainyun.com/jackie_" target="_blank"><img src="/img/rainyun/1.png" alt="雨云广告" style="max-width:100%;height:100%;object-fit:contain;"></a>';
    }
  }, 1000);
});

// 适配Next主题的手动切换明暗模式
document.addEventListener('theme-change', function() {
  // 模式切换时自动适配样式（CSS已通过[data-theme]实现，此处无需额外逻辑）
});
</script>